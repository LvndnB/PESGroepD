#include <stdio.h>
#include <sys/socket.h>

int main() {
  int sockfd = socket(AF_INET, SOCK_STREAM, 0);
  if (sockfd < 0) {
    perror("Kon de socket niet aanmaken!");
    return 1;
  }
    printf("Socket is aangemaakt...")

}
